# Data API

## API

Endpoints:

* `/` - Server info and status
* `/api/metadata/{id}`
* `/api/directory/{id}`
* `/api/text_file/{id}?path={path}`
* `/api/binary_file/{id}?path={path}`

## MCP

Endpoint: `/mcp/mcp`
